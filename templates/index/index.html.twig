{% extends 'logged.html.twig' %}

{% block title %}Hello IndexController!{% endblock %}

{% block body %}

    <div class="example-wrapper">
    <div class="container">
    <div class="row mt-5">
        {% for plan in plans %}
        {{ include("plan/partials/card.html.twig") }}
        {% if loop.index == 4 %}
    </div>
    <div class="row mt-5">
        {% endif %}
        {% endfor %}
    </div>
{% endblock %}